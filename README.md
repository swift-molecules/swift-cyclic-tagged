# swift-cyclic-tagged

Tagged integration for static Cyclic group elements. The package adds
tag-preserving construction, modular arithmetic, compound assignment, and
inversion without moving either domain's core behavior into the other atom.
